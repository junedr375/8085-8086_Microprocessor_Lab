;18COB323

DATA SEGMENT
    ARR DB 15,23,71,8,92,26,43,82,49
    LEN DW $-ARR
    MIN DB ?
    MAX DB ?
DATA ENDS
CODE SEGMENT
    ASSUME DS:DATA CS:CODE
START:
    MOV AX,DATA
    MOV DS,AX
    LEA SI,ARR
    MOV AL,ARR[SI]
    MOV MIN,AL
    MOV MAX,AL
    MOV CX,LEN
LOOP1:
    MOV AL,ARR[SI]
    CMP MIN,AL
    JL CHECK
    MOV MIN,AL
CHECK:
    CMP MAX,AL
    JG DONE
    MOV MAX,AL
DONE:
    INC SI
    LOOP LOOP1
    MOV AH,4CH
    INT 21H
CODE ENDS
END START